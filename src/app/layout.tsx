import type { Metadata, Viewport } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  maximumScale: 5,
};

export const metadata: Metadata = {
  title: "FITASC Sporting Romania",
  description:
    "Clasamente, competitii si rezultate pentru tirul sportiv FITASC Sporting si Compak Sporting din Romania.",
  keywords: ["FITASC", "sporting", "tir", "competitii", "clasamente", "Romania", "compak"],
  openGraph: {
    title: "FITASC Sporting Romania",
    description: "Clasamente si competitii de tir sportiv",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="ro">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
